# En desarrollo dejar vacío — Vite usa el proxy de vite.config.js hacia http://localhost:8000
# En producción (Vercel) poner la URL pública del backend de Render, sin slash final:
# VITE_API_URL=https://nexlayer-api.onrender.com
VITE_API_URL=
